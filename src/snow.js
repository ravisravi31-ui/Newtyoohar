(function(){function init(){const canvas=document.createElement('canvas');canvas.id='snow-canvas';canvas.style.position='fixed';canvas.style.inset='0';canvas.style.pointerEvents='none';canvas.style.zIndex='40';document.body.appendChild(canvas);const ctx=canvas.getContext('2d');let w,h;function resize(){w=canvas.width=window.innerWidth;h=canvas.height=window.innerHeight}resize();window.addEventListener('resize',resize);const flakes=[];const count=Math.floor((window.innerWidth+window.innerHeight)/120);for(let i=0;i<count;i++)flakes.push({x:Math.random()*w,y:Math.random()*h,r:Math.random()*3+1,dx:Math.random()*1-0.5,dy:Math.random()*1+0.3});function update(){ctx.clearRect(0,0,w,h);ctx.fillStyle='rgba(255,255,255,0.9)';flakes.forEach(f=>{f.x+=f.dx;f.y+=f.dy; if(f.y>h){f.y=-10;f.x=Math.random()*w} ctx.beginPath();ctx.arc(f.x,f.y,f.r,0,Math.PI*2);ctx.fill();});requestAnimationFrame(update);}update();}if(document.readyState==='complete')init();else window.addEventListener('load',init);})();